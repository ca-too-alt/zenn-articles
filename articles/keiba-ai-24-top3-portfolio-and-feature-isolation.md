title: "競馬AI開発記録 #24 単勝特化の限界を「複勝専用モデル」で突破する：特徴量空間の物理的隔離"emoji: "🏆"type: "tech"topics: ["python", "machinelearning", "ai", "horse_racing", "lightgbm"]published: false

# 🏆 競馬AI開発記録 #24：単勝特化の限界を「複勝専用モデル」で突破する：特徴量空間の物理的隔離

## 📄 概要と課題提起 (The Problem)

前回、我々は単勝ROIが85.17%という頭打ちに直面しました。この根本原因は、本命帯（Favorite）の確率推定における**「物理的な上限」**にあることが判明しました。

単一のランキングモデルによる全方位的な予測には限界があり、特に2着や3着といった「複勝圏内」に粘り込む馬の評価において、市場が持つオッズ順の情報識別能に劣るという構造的な課題が浮き彫りになりました。

本稿（第24話）では、この課題を解決するため、単勝モデルに加え"**「複勝専用モデル (Top3 Model)」**"を導入し、単勝と複勝の予測結果を組み合わせたポートフォリオによる収益安定化を目指した試行錯誤のプロセスを記録します。特に、性質が異なる複数の専門モデルを共存させる際に発生する"**「特徴量空間の干渉（Feature Space Interference）」**"という、マルチモデル・アーキテクチャ特有の難問とその解決策に焦点を当てて詳述します。

---

## ⚙️ 実装アプローチ：多層的ポートフォリオへの移行 (The Solution Architecture)

従来の「ランキングモデル一本」という単一構成を廃止し、目的変数の異なる複数の専門モデルを連携させる**マルチモデル・アーキテクチャ**へと根本的に移行しました。この設計は、以下の3つの柱で成り立っています。

### 1. モデルの役割分担と特徴量セットの専門化
各モデルに最適な「視点（眼）」を持たせるため、入力される特徴量セットを論理的かつ物理的に分離することが必須となりました。

*   **🥇 Ranking Model (単勝特化)**:
    *   目的関数：`rank_xendcg` を継続使用し、1着予測に特化。
    *   重視する特徴量：`prev_performance_score`, `ewma_rank` など、1着との相関が高い指標群。
*   **🥈 Top3 Model (複勝圏内特化)**:
    *   目的関数：3着以内に入る確率を最大化するためのバイナリ分類モデル。
    *   重視する特徴量：`jockey_place_rate`（騎手複勝率）、`avg_placed_5races`（過去5走の複勝圏内率）など、複勝圏内への突入に特化した指標群。

### 2. 数学的処理の継続
ランク学習モデルから得られるスコア $s_i$ を、最終的な確率 $\text{P}_i$ に変換するため、引き続きPlackett-Luceモデルに基づいたSoftmax関数を採用しました。

$$\text{P}_i = \frac{\exp(s_i / T)}{\sum_{j \in Race} \exp(s_j / T)}$$
*（注：この式は、スコアを確率に変換する際の基本的な構造を示しています。）*

### 3. システムアーキテクチャの変遷

システムが抱える課題と解決策を視覚的に理解するため、データフロー図を用いて比較します。

```mermaid
graph LR
    subgraph Old Architecture (Before: Single Global Feature Space)
        A[Raw Data] --> B(Feature Engineering);
        B --> C{Global Feature Fingerprint};
        C --> D1[Model A: Ranking];
        C --> D2[Model B: Top3];
        D1 & D2 --> E(Single Calibration/Integration Layer);
    end

    subgraph New Architecture (After: Isolated Model Spaces)
        A --> B;
        B --> C_rank{Rank Feature Space};
        B --> C_top3{Top3 Feature Space};
        C_rank --> D1[Model A: Ranking];
        C_top3 --> D2[Model B: Top3];
        D1 & D2 --> E_full(X_full: Full Column Data);
        E_full --> F(Score Integrator / Calibration Layer);
    end

    style Old Architecture fill:#fdd,stroke:#c00,stroke-width:2px
    style New Architecture fill:#dfd,stroke:#090,stroke-width:2px
```

---

## 🚨 遭遇した問題：特徴量空間の「NaN汚染」とROIの崩壊 (The Crisis)

Top3モデル単体の精度（AUC 0.7292）は順調に向上しましたが、統合テストにおいて致命的な問題が発生しました。

**【原因】**
これまで、すべてのモデルが単一のグローバル変数である `feature_fingerprint` （特徴量の指紋）を共有していました。この構造により、Top3専用列（例：複勝率関連）が意図せず混入し、特に単勝用の確率校正器（Isotonic Regression）の入力データに「知らない列」がNaNとして汚染されました。

**【結果】**
この`NaN汚染`の影響を受け、最も重要な指標である `calibrated_prob_win` （校正済み勝率）の計算が歪み、単勝ROIは当初の85.17%から67.38%へと、約20ポイントも崩壊するという壊滅的な事態に陥りました。

---

## ✅ 解決アプローチ：物理的・論理的分離による再設計 (The Fix)

この危機を乗り越えるため、単一のデータパイプラインで複数のモデルを扱うという根本的な限界を認め、以下の3段階にわたって特徴量空間を徹底的に隔離しました。

1.  **論理的分離（Fingerprintの純化）**:
    *   `model_trainer.py` の `prepare_training_data` 関数内で、`feature_fingerprint` の定義を「ランキング・穴馬用」に厳格に限定し、Top3専用列をデータロード段階で物理的に排除しました。これにより、校正器の入力が純粋な単勝関連特徴量のみとなることを保証しました。
2.  **物理的分離（Calibration Historyの多層化）**:
    *   モデルごとの校正用データを保持する `calib_history` の構造を拡張し、ランキング用のデータセット `'X'` と、複勝モデル専用のデータセット `'X_top3'` を別々に保持する設計を導入しました。これにより、異なる目的を持つモデルが互いの学習履歴を汚染することがなくなりました。
3.  **推論パスの拡張（全列データの受け渡し）**:
    *   `score_integrator.py` から `prediction.py` へとデータを受け渡すインターフェースを新設しました。この新しいパイプラインは、特定の指紋に縛られない「全列データ (`X_full`)」を受け取ることを可能にし、各モデルが自身に必要な特徴量のみを選択的に参照できる柔軟な状態を実現しました。

---

## ✨ 最終的な成果と結論 (The Outcome)

アーキテクチャの基盤を刷新した結果、以下の数値的・構造的な安定性を確立することができました。

*   **Top3モデルの識別能**: `prob_top3` AUC は目標値0.72を超える **0.7213** を達成しました。
*   **単勝識別能の回復**: 特徴量汚染が解消された結果、In-Race Spearman 相関は **0.2527** （exp023比で改善）を記録し、信頼性を大幅に向上させました。
*   **ROIの安定化**: 汚染によって崩壊した単勝ROIを、v2水準（85.17%以上）へ復旧させる強固な土台が完成しました。

---

## 💡 学びと今後の展望 (Lessons Learned & Next Steps)

### 🧠 ランキングモデルの繊細さと「指紋」の二面性
今回の経験から、ランキングモデルの校正器は極めて繊細であり、「常にNaNを含む列」という単なるデータ構造の問題が、システムの収益性を即座に破壊し得ることを痛感しました。

これまでの `feature_fingerprint` は、「物理的なデータロード用」と「論理的な校正入力の構造規定用」という二つの役割を兼ねていましたが、この**役割の衝突こそが今回のバグの本質**でした。「モデルごとに独立した列空間を管理する」という設計は、単なるリファクタリングではなく、精度の高い期待値運用を行うための絶対条件であることを再認識しました。

### 🚀 次回予告：アーキテクチャの恒久的な解決へ
特徴量空間の分離により、複数のモデルが互いを汚染することなく共存できる基盤は整いました。しかし、現在の実装はまだ「場当たり的なパッチ」に近い状態です。

次回は、今回得られた知見をコア基盤へと昇格させます。`ModelConfig` と `feature_spaces` 辞書を導入することで、システムの拡張性を恒久的に解決する"**「exp025: アーキテクチャ刷新編」**"をお届けします。
